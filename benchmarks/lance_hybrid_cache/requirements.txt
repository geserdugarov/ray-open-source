# pylance is installed separately from $HOME/git/lance-open-source. Pin the
# checkout to the Lance 6.0 distributed-cache branch (`private-cache-6.0-ver-1`
# at commit `9ebfe4de0` or newer); the v6 `Session.with_distributed_cache`,
# `Session.invalidate_index_cache`, `Session.size_bytes`, and the strict
# `dataset.prewarm_index(name, partition_ids=...)` path are all required by the
# scenarios + drivers in this directory. On that branch the feature is
# baked into the wheel and a plain editable install is sufficient:
#   pip install -e "$HOME/git/lance-open-source/python"
# Re-running `pip install -e` (or `maturin develop --release`) is only
# required after Rust changes. The sharded-mode actor paths additionally
# require pylance to expose `dataset.compute_partition_ids` and
# `dataset.search_partitions`; without those wrappers the actor raises a
# clear RuntimeError on first use. See
# plans/benchmark/lance-v6-api-verification.md for the current binding
# status against a candidate pylance build.
ray[default]>=2.40
lance-namespace>=0.5.2
numpy>=1.26
pyarrow>=15
matplotlib>=3.8
pandas>=2.2
